
<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark p-3 mb-3">
    <a class="navbar-brand text-primary" href={{URL_SITE}}>{{site_name}} {% if app.user %}</br> <small class="text-light" >Bonjour {{ app.user.firstName }} {{ app.user.lastName }} <span></span></small> {% endif %}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link isActive" href="{{ path('account') }}">Espace personnel<span></span></a>
            </li>
            {% endif %}
            {# <li class="nav-item">
                <a class="nav-link" href="#">Features</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Dropdown link
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" href="#">Action</a>
                <a class="dropdown-item" href="#">Another action</a>
                <a class="dropdown-item" href="#">Something else here</a>
                </div>
            </li> #}
            {% if  app.user %}
                <li class="nav-item">
                    <a class="nav-link text-danger" href={{ path('app_logout') }}>Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link text-success" href={{ path('app_login') }}>Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
